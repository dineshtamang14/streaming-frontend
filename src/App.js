import React, { useState } from 'react';\n\nconst App = () => {\n  const [movies, setMovies] = useState([]);\n  const [songs, setSongs] = useState([]);\n  const [searchTerm, setSearchTerm] = useState('');\n\n  const uploadMovie = (event) => {\n    const newMovie = event.target.files[0];\n    if (newMovie) {\n      setMovies([...movies, newMovie]);\n    }\n  };\n\n  const uploadSong = (event) => {\n    const newSong = event.target.files[0];\n    if (newSong) {\n      setSongs([...songs, newSong]);\n    }\n  };\n\n  const filteredMovies = movies.filter(movie => movie.name.toLowerCase().includes(searchTerm.toLowerCase()));\n  const filteredSongs = songs.filter(song => song.name.toLowerCase().includes(searchTerm.toLowerCase()));\n\n  return (\n    <div>\n      <h1>Media Uploads</h1>\n      <input\n        type='text'\n        placeholder='Search...'\n        value={searchTerm}\n        onChange={(e) => setSearchTerm(e.target.value)}\n      />\n      <div>\n        <h2>Upload Movies</h2>\n        <input type='file' onChange={uploadMovie} />\n        <ul>{filteredMovies.map((movie, index) => <li key={index}>{movie.name}</li>)}</ul>\n      </div>\n      <div>\n        <h2>Upload Songs</h2>\n        <input type='file' onChange={uploadSong} />\n        <ul>{filteredSongs.map((song, index) => <li key={index}>{song.name}</li>)}</ul>\n      </div>\n    </div>\n  );\n};\n\nexport default App;